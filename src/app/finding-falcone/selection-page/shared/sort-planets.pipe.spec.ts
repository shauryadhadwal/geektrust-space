import { SortPlanetsPipe } from './sort-planets.pipe';

describe('SortPlanetsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPlanetsPipe();
    expect(pipe).toBeTruthy();
  });
});
